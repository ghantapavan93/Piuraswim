import { CartDrawer } from "@/components/commerce/CartDrawer";
import { SiteFooter } from "@/components/layout/SiteFooter";
import { SiteHeader } from "@/components/layout/SiteHeader";
import { BETWEEN_SIZES, MEASURING_NOTE, MEASURING_STEPS, SIZE_CHART } from "@/data/fit";

export default function SizeGuidePage() { return <><SiteHeader /><main className="guide-page"><div className="container"><header><p className="label accent">Find your Piura fit</p><h1 className="display display-lg">Made to feel<br /><em>like yours.</em></h1><p>Every Piura piece runs true to size. Measure once, then wear it everywhere.</p></header><section className="guide-table"><table><caption>Body measurements in inches</caption><thead><tr><th>Piura size</th><th>Bust</th><th>Waist</th><th>Hips</th></tr></thead><tbody>{SIZE_CHART.map((row) => <tr key={row.size}><th>{row.size}</th><td>{row.bust.join("–")}</td><td>{row.waist.join("–")}</td><td>{row.hips.join("–")}</td></tr>)}</tbody></table></section><p className="guide-note">{MEASURING_NOTE}</p><section className="guide-measure"><h2 className="display display-sm">How to measure.</h2><ol>{MEASURING_STEPS.map((step) => <li key={step.key}><span>{step.letter}</span><div><h3>{step.name}</h3><p>{step.how}</p></div></li>)}</ol></section><section className="guide-between"><p className="label accent">Between sizes?</p><p>{BETWEEN_SIZES}</p></section></div></main><SiteFooter /><CartDrawer /></>; }
